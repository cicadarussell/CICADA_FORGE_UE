# Unreal Project Folder

Create the UE5.8 project here later.

Suggested local path:

`C:\CICADA\CICADA_APPS\CICADA_FORGE_UE`

Suggested project name:

`CICADAForgeUE`

Recommended workflow:
1. Create a blank UE5.8 C++ project.
2. Put it inside this repo or copy this repo into the project root.
3. Keep `Config/`, `Content/`, `Source/`, `Plugins/`, `.uproject`.
4. Do not commit `Intermediate/`, `Saved/`, `DerivedDataCache/`, local build junk.
