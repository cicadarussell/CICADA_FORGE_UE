git init
git add .
git commit -m "Bootstrap CICADA FORGE UE project spine"
git branch -M main

Write-Host "Now create an empty GitHub repo named CICADA_FORGE_UE, then run:"
Write-Host "git remote add origin https://github.com/cicadarussell/CICADA_FORGE_UE.git"
Write-Host "git push -u origin main"
